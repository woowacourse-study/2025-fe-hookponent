{
  "private": true,
  "workspaces": [
    "packages/*",
    "docs"
  ],
  "scripts": {
    "build:hookdle": "npm run build --workspace=hookdledle",
    "dev:docs": "npm run start --workspace=docs"
  }
}
