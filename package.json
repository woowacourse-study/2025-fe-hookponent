{
  "private": true,
  "workspaces": [
    "packages/*",
    "docs"
  ],
  "scripts": {
    "build:hookdle": "npm run build --workspace=hookdle",
    "dev:docs": "npm run start --workspace=docs"
  }
}
